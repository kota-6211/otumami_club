
<div>

  <div class="section-border1"></div>

  <div class="main-image d-flex justify-content-center">
   <%= image_tag 'main-image.jpg', width: '100%', height: '100%' %>
  </div>
  <div class="section-border"></div>

  <div class="headline-text">
    <p>自分のおつまみを皆とシェアしたり、</p>
    <p>他のメンバーから新しいアイデアを見つけることもできます。</p>
    <p>好きなお酒に合うおつまみを見つけて、家呑みをより楽しんでみませんか？</p>
    <p>おつまみに迷った時は、家にある食材でできるおつまみを『おつまみメーカー』が提案します。</p>
    <p>ー 家呑みをもっと楽しく、もっとおいしく ー</p>
  </div>

  <div class="section-border"></div>

  <div class="pickup d-flex flex-column ">
    <h3 class="text-top">Pick Upおつまみ</h3>
      <div class="pickup-recipe-box">
        <% if @pick_up_recipes.any? %>
          <% @pick_up_recipes.each do |recipe| %>
            <div class="pickup-recipe">
              <%= link_to recipe_path(recipe) do%>
                <%= image_tag recipe.get_recipe_image(200,200), width: '80%', height: '80%' %>
                <div class="pickup-recipe-text">
                  <h4 class="pickup-title"><%= recipe.title %></h4>
                  <h5 class="pickup-body"><%= recipe.body %></h5>
                </div>
              <% end %>
            </div>
          <% end %>
        <% else %>
          no-data
        <% end %>
      </div>
  </div>

  <div class="section-border"></div>

  <div class="tags">
    <h3 class="text-top">タグで探す</h3>
    <% @tags.each do |tags| %>
      <%= link_to tags.name, tag_path(tags), class: "btn btn-outline-dark tags-text"%>
    <% end %>
  </div>

  <div class="section-border"></div>
  <div class="beer_recipe d-flex justify-content-center flex-wrap">
    <div class="box box1 d-flex justify-content-center">
       <p><span class="beer">ビール</span><br>とつまむ</p>
    </div>

    <% if AlcoholGenre.any? %>
      <% @beer.each do |beer_recipe| %>
        <div class="box box2">
          <%= link_to recipe_path(beer_recipe) do %>
            <div class="box-image"><%= image_tag beer_recipe.get_recipe_image(200,200), width: '60%', height: '60%' %></div>
            <div class="box-text "><%= beer_recipe.title %></div>
          <% end %>
        </div>
      <% end %>
    <% end %>
    <div class="box box1 d-flex justify-content-center beer-bg">
      <%= image_tag 'beer.jpg', width: '100%', height: '100%' %>
    </div>
  </div>

  <div class="section-border"></div>

  <div class="beer_recipe d-flex justify-content-center flex-wrap">
    <div class="box box1 d-flex justify-content-center">
       <%= image_tag 'wine.jpg', width: '100%', height: '100%' %>
    </div>
    <% if AlcoholGenre.any? %>
      <% @wine.each do |wine_recipe| %>
        <div class="box box2">
          <%= link_to recipe_path(wine_recipe) do %>
            <div class="box-image"><%= image_tag wine_recipe.recipe_image, width: '60%', height: '60%' %></div>
            <div class="box-text"><%= wine_recipe.title %></div>
          <% end %>
        </div>
      <% end %>
    <% end %>
    <div class="box box1 d-flex justify-content-center wine-bg">
      <p><span  class="beer">ワイン</span><br>に合わせる</p>
    </div>
  </div>

  <div class="section-border"></div>

  <div class="favorite-ranking">
    <h3 class="text-top favorite-text-top">人気のおつまみ</h3>
    <div class="">
      <div class="favorite-recipe d-flex">
        <% @recipe_favorites.each do |favorite_recipe| %>
          <div class="favorite-recipe-content" >
            <%= link_to recipe_path(favorite_recipe) do %>
              <%= image_tag favorite_recipe.get_recipe_image(170,170), width: '70%', height: '70%', class: "favo-img" %>
              <p class="favorite-text"><%= favorite_recipe.title %></p>
              <p class="text-dark"><%= "#{favorite_recipe.favorites.count}" + "♡" %></p>
            <% end %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
<div class="start">
    <p class="start-img"><%= image_tag asset_path('logo10.png') %></p>
</div>

<script>
  $(function() {
	setTimeout(function(){
		$('.start p').fadeIn(1600);
	},500); //0.5秒後にロゴをフェードイン!
	setTimeout(function(){
		$('.start').fadeOut(1000);
	},2500); //2.5秒後にロゴ含め真っ白背景をフェードアウト！
});
</script>